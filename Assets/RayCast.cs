using UnityEngine;
using System.Collections;

public class RayCast : MonoBehaviour {
	public GameObject leap;
	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
		//print ("lllo");
		//RaycastHit rayHit;
		//Ray ray = camera.ScreenPointToRay (leap.transform.position);
		//Debug .DrawRay( ray.origin , ray.direction * 1000 , Color.red ) ; 
		//Debug.DrawLine(Camera.main.ScreenPointToRay(Input.mousePosition).origin,rayHit.point, Color.re);
	
	}
}
